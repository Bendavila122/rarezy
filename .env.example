# Copy this file to .env.local and fill in your Supabase project's values
# (Project Settings → API in the Supabase dashboard). Until these are set,
# the app runs the login/signup flow in demo mode — see src/lib/auth.ts.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
